name: shadcn2fluid/pricing-comparison
typeName: shadcn2fluid_pricingcomparison
title: Pricing Comparison
description: Feature comparison table across pricing plans
group: pricing
prefixFields: false
fields:
  - identifier: header
    useExistingField: true
    label: Heading

  - identifier: eyebrow
    type: Textarea
    rows: 1
    label: Eyebrow

  - identifier: plans
    type: Collection
    label: Plan Names
    minItems: 2
    maxItems: 4
    fields:
      - identifier: name
        type: Textarea
        rows: 1
        label: Plan Name
        required: true

      - identifier: price
        type: Textarea
        rows: 1
        label: Price

  - identifier: feature_items
    type: Collection
    label: Feature Rows
    minItems: 1
    fields:
      - identifier: feature_name
        type: Textarea
        rows: 1
        label: Feature Name
        required: true

      - identifier: tier_values
        type: Textarea
        rows: 1
        label: Values (comma-separated)
        description: One value per plan, e.g. Yes, Yes, No
        required: true
